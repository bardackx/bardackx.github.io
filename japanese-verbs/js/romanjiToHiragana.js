export default function romanjiToHiragana(romanji) {
    let hiragana = "";
    let newCursor = -1;
    let limbo = "";
    for (let i = 0; i < romanji.length; i++) {
    }
    return hiragana + limbo;
}
console.log(romanjiToHiragana("c"));
console.log(romanjiToHiragana("ch"));
console.log(romanjiToHiragana("cho"));
console.log(romanjiToHiragana("chot"));
console.log(romanjiToHiragana("chott"));
